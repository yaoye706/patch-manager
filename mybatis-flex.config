processor.enable=true # 全局启用apt开关
processor.mapper.annotation=true # 开启 @Mapper 注解
processor.mapper.generateEnable=false # mapper自动生成，flex代码生成就重复了。需要关闭一个
processor.tableDef.propertiesNameStyle=lowerCase